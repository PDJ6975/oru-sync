model User {
    id Int @id @default(autoincrement())
    sessions Session[]
    name String
    lastComputedDay DateTime?
    dailyBonusAplied Boolean @default(false)
    stats UserStats[]
    habits Habit[]
    units Unit[]
    assignments Assignment[]
    timerSessions TimerSession[]
}