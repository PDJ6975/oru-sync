services:
  postgres:
    image: postgres:18.1
    restart: always
    environment:
      POSTGRES_DB: oru
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: secret
    ports:
      - "5432:5432"
    volumes:
      - postgres:/var/lib/postgresql/data

volumes:
  postgres:
