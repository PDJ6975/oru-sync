model Compliance {
    id String @id
    date DateTime
    isCompleted Boolean
    recordedAmount Float?
    habit Habit @relation(fields: [habitId], references: [id], onDelete: Cascade)
    habitId String
    @@unique([habitId, date])
}